/**
 * Enum representing different API endpoints.
 * @enum {string} ENDPOINT
 */
enum ENDPOINT {
    /**
     * Represents the login endpoint.
     * @type {string}
     * @memberof ENDPOINT
     */
    "LOGIN" = "login"
}